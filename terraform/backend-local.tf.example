# Local Backend Configuration (for testing/validation only)
# This file provides a local backend alternative to HCP Terraform
# for syntax validation and local testing purposes.
#
# To use local backend:
# 1. Comment out the 'cloud' block in versions.tf
# 2. Copy this file to backend-local.tf
# 3. Run terraform init
#
# WARNING: This is NOT recommended for production use.
# Always use HCP Terraform (remote backend) for actual deployments.

terraform {
  backend "local" {
    path = "terraform.tfstate"
  }
}
